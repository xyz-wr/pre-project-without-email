spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    hibernate:
      ddl-auto: update  # (1) ??? ?? ?? ?? ??? ??? ?? ???. ?? ??? ? ???
    show-sql: true      # (2) SQL ?? ??